/**
 * Imports the accent-colour CSS file so it is bundled by Next.js.
 * Mount this component once in the app layout chain.
 */
import "../app/accent-colour.css";

export default function AccentColourStyleMount() {
  return null;
}
