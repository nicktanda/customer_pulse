<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Customer Pulse" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-screen bg-gray-50">
    <% if user_signed_in? %>
      <nav class="bg-indigo-600 shadow-lg">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex items-center">
              <%= link_to root_path, class: "flex items-center text-white font-bold text-xl" do %>
                <svg class="h-8 w-8 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
                </svg>
                Customer Pulse
              <% end %>

              <div class="hidden md:flex ml-10 space-x-4">
                <%= link_to "Dashboard", root_path, class: "text-white hover:bg-indigo-500 px-3 py-2 rounded-md text-sm font-medium #{request.path == root_path ? 'bg-indigo-700' : ''}" %>
                <%= link_to "Feedback", feedback_index_path, class: "text-white hover:bg-indigo-500 px-3 py-2 rounded-md text-sm font-medium #{request.path.start_with?('/feedback') ? 'bg-indigo-700' : ''}" %>
                <%= link_to "Integrations", integrations_path, class: "text-white hover:bg-indigo-500 px-3 py-2 rounded-md text-sm font-medium #{request.path.start_with?('/integrations') ? 'bg-indigo-700' : ''}" %>
                <%= link_to "Recipients", recipients_path, class: "text-white hover:bg-indigo-500 px-3 py-2 rounded-md text-sm font-medium #{request.path.start_with?('/recipients') ? 'bg-indigo-700' : ''}" %>
                <%= link_to "Reports", pulse_reports_path, class: "text-white hover:bg-indigo-500 px-3 py-2 rounded-md text-sm font-medium #{request.path.start_with?('/pulse_reports') ? 'bg-indigo-700' : ''}" %>
                <% if current_user.admin? %>
                  <%= link_to "Settings", settings_path, class: "text-white hover:bg-indigo-500 px-3 py-2 rounded-md text-sm font-medium #{request.path.start_with?('/settings') ? 'bg-indigo-700' : ''}" %>
                <% end %>
              </div>
            </div>

            <div class="flex items-center space-x-4">
              <span class="text-indigo-200 text-sm hidden sm:block">
                <%= current_user.name || current_user.email %>
                <% if current_user.admin? %>
                  <span class="bg-indigo-800 text-xs px-2 py-0.5 rounded ml-1">Admin</span>
                <% end %>
              </span>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-white hover:bg-indigo-500 px-3 py-2 rounded-md text-sm font-medium" %>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <main class="<%= user_signed_in? ? 'max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8' : '' %>">
      <% if notice %>
        <div class="mb-4 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
          <span class="block sm:inline"><%= notice %></span>
        </div>
      <% end %>

      <% if alert %>
        <div class="mb-4 bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
          <span class="block sm:inline"><%= alert %></span>
        </div>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
